<% breadcrumb :qiita %>
<div class="bg-qiita font-qiita items-center justify-center text-center pt-20 pb-12">
  <div class="text-secondary text-4xl pb-2">Railsちゃんねる</div>
  <div class="text-secondary text-2xl pb-8">一昨日から今日にかけて投稿された「Rails」タグが付いたQiita記事一覧</div>
  <div class="font-qiita">
    <% @articles.each do |article| %>
      <ul class="mb-8 link link-hover text-lg"><%= link_to "・#{article['title']}", "#{article['url']}", target: :_blank, rel: "noopener noreferrer" %></ul>
    <% end %>
  </div>
</div>
